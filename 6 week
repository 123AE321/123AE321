某些程式語言是以模板為主，而有些會是以程式為主，中間插一些模板

前端是送給瀏覽器的資料，後端的資料無法從開發人員選向看到

template跟樣板字符串很像，不過它屬於樣板包程式，可以在樣板裡面加程式

ejs是JS檔案的模板引擎，djs是deno檔案的模板引擎

不同瀏覽器有不同的session，可以讓不同用戶使用

語言本身其實不會造成安全性的問題，有問題可能出在內部管理

從08-fetch開始就是web2.0，不是一直換頁，而是用書籤

render : 呈現，丟回給前端，使用模板加裡面的參數，最後再丟回瀏覽器

#使用不同的模板會導致使用的語法不同
